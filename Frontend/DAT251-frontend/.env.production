# Production (mode) environment variables, used by vite when building the Svelte frontend app
VITE_API_BASE_URL=http://backend:8080    # This is the URL that Vite should bake into the Svelte app. Aka where the svelte app sends it's API request towards the Spring boot REST API
CYPRESS_WEB_SERVER_OF_SPA=http://localhost:5173   # This should point to where the web server serving the Svelte SPA / HTML from. This is the URL that Cypress is using as a baseurl for interacting with the website. Note that Cypress does not automatically use this file for its env, which is annoying. 